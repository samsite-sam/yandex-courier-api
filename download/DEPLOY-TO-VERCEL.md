# 🚀 Инструкция по деплою на Vercel

## Подготовка проекта завершена! ✅

Проект готов к развертыванию на Vercel. Вот два способа сделать это:

---

## Способ 1: Через GitHub (Рекомендуется)

### Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на [github.com](https://github.com) и войдите в аккаунт
2. Нажмите **"+" → "New repository"**
3. Назовите репозиторий, например: `yandex-courier-api`
4. Выберите **Private** или **Public**
5. Нажмите **"Create repository"**

### Шаг 2: Загрузите код на GitHub

В терминале выполните:

```bash
cd /home/z/my-project

# Инициализация git (если ещё не инициализирован)
git init

# Добавляем все файлы
git add .

# Создаём коммит
git commit -m "Initial commit: Yandex Courier Article Generator API"

# Добавляем remote (замените USERNAME на ваш GitHub логин)
git remote add origin https://github.com/USERNAME/yandex-courier-api.git

# Загружаем на GitHub
git push -u origin main
```

### Шаг 3: Подключите Vercel

1. Перейдите на [vercel.com](https://vercel.com)
2. Нажмите **"Sign Up"** или **"Log In"** (можно войти через GitHub)
3. После входа нажмите **"Add New..." → "Project"**
4. Выберите ваш репозиторий `yandex-courier-api`
5. Vercel автоматически определит Next.js проект
6. Нажмите **"Deploy"**

### Шаг 4: Получите URL

После деплоя (1-2 минуты) вы получите URL вида:
```
https://yandex-courier-api.vercel.app
```

---

## Способ 2: Через Vercel CLI (Быстрее)

### Шаг 1: Установите Vercel CLI

```bash
npm install -g vercel
# или
bun install -g vercel
```

### Шаг 2: Авторизуйтесь

```bash
vercel login
```

### Шаг 3: Разверните проект

```bash
cd /home/z/my-project
vercel --prod
```

Vercel задаст несколько вопросов - нажмите Enter для всех (значения по умолчанию подойдут).

### Шаг 4: Получите URL

После деплоя вы увидите URL в терминале.

---

## Настройка WordPress-плагина

После успешного деплоя:

1. В WordPress админке перейдите в **АвтоПостинг → Настройки**
2. В поле **API Endpoint** введите:
   ```
   https://ваш-проект.vercel.app/api/article-generator
   ```
3. Нажмите **"Проверить соединение"**
4. Сохраните настройки

---

## Проверка API

### Проверка статуса (GET запрос)

Откройте в браузере:
```
https://ваш-проект.vercel.app/api/article-generator/generate
```

Должны увидеть:
```json
{
  "status": "ok",
  "service": "Yandex Courier Article Generator",
  "version": "1.0.0",
  "topicsCount": 20
}
```

### Генерация статьи (POST запрос)

```bash
curl -X POST https://ваш-проект.vercel.app/api/article-generator/full \
  -H "Content-Type: application/json" \
  -d '{}'
```

---

## Файловая структура проекта

```
/home/z/my-project/
├── src/app/
│   ├── page.tsx                          # Главная страница
│   └── api/article-generator/
│       ├── generate/route.ts             # Проверка статуса
│       ├── image/route.ts                # Поиск изображений
│       └── full/route.ts                 # Полная генерация
├── next.config.ts                        # Конфигурация Next.js
├── vercel.json                           # Конфигурация Vercel
├── package.json                          # Зависимости
└── bun.lockb                             # Lock-файл
```

---

## Лимиты Vercel (Free план)

- ✅ 100 GB пропускной способности/месяц
- ✅ 1000 вызовов Serverless Functions/день
- ✅ 10 секунд на выполнение функции
- ✅ Автоматический HTTPS

Для генерации 3 статей в день лимитов более чем достаточно!

---

## Возможные проблемы

### "Function Timeout"
- Функция выполняется дольше 10 секунд
- Решение: перейдите на Pro план или оптимизируйте код

### "Build Failed"
- Проверьте `bun.lockb` файл
- Убедитесь, что все зависимости в `package.json`

### "API недоступен"
- Проверьте правильность URL в плагине
- Убедитесь, что деплой прошёл успешно

---

## Поддержка

При возникновении вопросов:
- Документация Vercel: https://vercel.com/docs
- Документация Next.js: https://nextjs.org/docs
